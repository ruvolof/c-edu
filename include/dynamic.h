#ifndef dynamic
#define dynamic

extern int LongestCommonSequence(char* a, char* b);

extern int Partition(int* nums, int length);

extern int Knapsack(int *values, int *weight, int length, int capacity);

extern int EditDistance(char* a, char* b);

#endif
